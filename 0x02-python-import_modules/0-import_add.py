mport_add.py

import add_0

# Assign the value 1 to a variable called a
a = 1

# Assign the value 2 to a variable called b
b = 2

# Call the function add and print the result with string format
print(f"{a} + {b} = {add_0.add(a, b)}")
